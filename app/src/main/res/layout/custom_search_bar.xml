<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardElevation="4dp" >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageView
            android:id="@+id/image_view_back"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_arrowleft"
            android:tint="#666666"
            android:contentDescription="Kembali"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:padding="4dp" />

        <!-- Search Bar -->
        <EditText
            android:id="@+id/edit_text_search"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="@drawable/search_background"
            android:hint="Cari di Klik Indomaret"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:textSize="16sp"
            android:drawableStart="@drawable/ic_search"
            android:drawableEnd="@drawable/ic_close"
            android:drawablePadding="20dp"
            android:singleLine="true"
            android:imeOptions="actionSearch"
            android:layout_toEndOf="@id/image_view_back"
            android:layout_marginStart="12dp"
            android:inputType="text"
            android:textCursorDrawable="@drawable/custom_cursor"
            android:textColorHighlight="@color/yellow"
            android:textSelectHandle="@drawable/cutom_handle"
            android:textSelectHandleLeft="@drawable/cutom_handle"
            android:textSelectHandleRight="@drawable/cutom_handle"/>
    </RelativeLayout>
</androidx.cardview.widget.CardView>
